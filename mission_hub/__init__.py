"""Ninereeds Mission Hub authoritative control plane."""

# Keep package import side-effect free so the trainbox release can contain the
# protocol/agent subset without the Mission Hub database implementation.
__version__ = "0.1.0"
