{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["bucket", "selected_caption_artifact_id", "evidence", "uncertainty", "reason"],
  "properties": {
    "bucket": {"enum": ["accept", "check_again", "reject"]},
    "selected_caption_artifact_id": {"type": ["string", "null"], "minLength": 1, "maxLength": 512},
    "evidence": {"type": "array", "items": {"type": "string"}},
    "uncertainty": {"type": "array", "items": {"type": "string"}},
    "reason": {"type": "string", "minLength": 1}
  },
  "additionalProperties": false
}
