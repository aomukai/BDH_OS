# Fresh anonymous Sol rehearsal reviewer - L000

Review only the anonymous review packet, its bound lesson/log/report/wiki inputs, and the verdict
schema. Do not infer model identities or seek the hidden simulator profile. You are a different
session from the learner simulator.

Audit every event and every required dimension. Check phase completeness, actor alternation,
language closure, visual operations, Point/Topic integrity, story and comprehension application,
intervention decisions, turn accounting, alarm judgment, developmental-stage fidelity, learner
simulation plausibility, and calibration of the teacher's report against raw evidence. A passing
run needs every dimension true, every required phase, complete teacher/student pairs, an explicit
understanding check, no alarm, and no failure. If teaching evidence is usable but the report is not,
use `sol_reconstruction_required` with exact corrections. Write only the declared verdict JSON.
