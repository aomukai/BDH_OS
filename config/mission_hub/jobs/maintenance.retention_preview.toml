schema_version = 1

[job]
id = "maintenance.retention_preview"
version = 1
description = "Produce a non-destructive retention proposal."
owner = "mission_hub"
executor_role = "mission_hub"
handler = "mission_hub.handlers.disabled:DisabledHandler"
enabled = false
requires_live_execution = false
priority = 10
timeout_seconds = 600
max_attempts = 1
retry_policy = "never"
input_schema = "schemas/mission_hub/jobs/generic.input.schema.json"
output_schema = "schemas/mission_hub/jobs/generic.output.schema.json"
artifact_types = ["retention_report"]
artifact_input_fields = []
required_capabilities = ["control"]
approval = "none"
provider_route = "deterministic"
prompt_id = ""
