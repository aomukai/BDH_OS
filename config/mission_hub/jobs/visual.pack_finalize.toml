schema_version = 1
[job]
id = "visual.pack_finalize"
version = 1
description = "Atomically finalize a fully reviewed immutable visual pack."
owner = "mission_hub"
executor_role = "mission_hub"
handler = "mission_hub.handlers.visual:VisualPackFinalizeHandler"
enabled = false
requires_live_execution = false
critical = true
priority = 65
timeout_seconds = 600
max_attempts = 1
retry_policy = "never"
input_schema = "schemas/mission_hub/jobs/visual-stage.input.schema.json"
output_schema = "schemas/mission_hub/jobs/visual-stage.output.schema.json"
artifact_types = ["visual_pack"]
required_artifact_types = ["visual_pack"]
artifact_input_fields = ["input_artifact_ids"]
required_capabilities = ["control"]
approval = "operator"
provider_route = "deterministic"
prompt_id = ""
