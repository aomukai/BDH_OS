schema_version = 1
[job]
id = "visual.experience_compile"
version = 1
description = "Compile accepted images and canonical text into ordered learner events."
owner = "mission_hub"
executor_role = "mission_hub"
handler = "mission_hub.handlers.visual:VisualExperienceCompileHandler"
enabled = false
requires_live_execution = false
critical = true
priority = 75
timeout_seconds = 600
max_attempts = 1
retry_policy = "never"
input_schema = "schemas/mission_hub/jobs/visual-stage.input.schema.json"
output_schema = "schemas/mission_hub/jobs/visual-stage.output.schema.json"
artifact_types = ["visual_experience"]
required_artifact_types = ["visual_experience"]
artifact_input_fields = ["input_artifact_ids"]
required_capabilities = ["control"]
approval = "operator"
provider_route = "deterministic"
prompt_id = ""
