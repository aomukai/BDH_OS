# Campaign material

Frozen curricula, image assignments, captions, feature archives, and other
campaign payloads are local Mission Hub artifacts. They are intentionally not
stored in Git or included in trainbox source releases.

The repository retains only the schemas, builders, validators, and deployment
contracts needed to reproduce and verify those artifacts. Runtime material is
registered by content hash in Mission Hub and backed up through the operational
artifact store.
